---
ansible-navigator:
  ansible:
    inventory:
      entries:
      - /home/iac-admin/test/inventory/hosts
  playbook-artifact:
    save-as: /home/iac-admin/test/logs/{playbook_name}.json
  color:
    enable: true
    osc4: true